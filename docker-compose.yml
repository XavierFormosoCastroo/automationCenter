services:
  automation-center:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./reports:/app/reports
      - ../myGoogleDrive:/projects/myGoogleDrive
